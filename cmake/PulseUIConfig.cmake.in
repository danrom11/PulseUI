# PulseUIConfig.cmake - generated from PulseUIConfig.cmake.in

@PACKAGE_INIT@

include(CMakeFindDependencyMacro)

set(PulseUI_VERSION "@PROJECT_VERSION@")

set(PulseUI_CONFIG_DIR "${CMAKE_CURRENT_LIST_DIR}")

set_and_check(PulseUI_INCLUDE_DIR "${PACKAGE_PREFIX_DIR}/@PACKAGE_INCLUDE_INSTALL_DIR@")

set(PULSEUI_INCLUDE_DIRS "${PulseUI_INCLUDE_DIR}")
set(PULSEUI_VERSION      "${PulseUI_VERSION}")

include("${CMAKE_CURRENT_LIST_DIR}/PulseUITargets.cmake")

set(PulseUI_MODULE_DIR "${CMAKE_CURRENT_LIST_DIR}")
list(APPEND CMAKE_MODULE_PATH "${PulseUI_MODULE_DIR}")

function(pulseui_include_warnings)
  include("${CMAKE_CURRENT_LIST_DIR}/compiler_warnings.cmake")
endfunction()

if (NOT DEFINED CMAKE_CXX_STANDARD)
  set(CMAKE_CXX_STANDARD 20)
  set(CMAKE_CXX_STANDARD_REQUIRED ON)
endif()

if (DEFINED PULSEUI_CONFIG_VERBOSE AND PULSEUI_CONFIG_VERBOSE)
  message(STATUS "Found PulseUI ${PulseUI_VERSION}")
  message(STATUS "  Includes: ${PulseUI_INCLUDE_DIR}")
endif()
